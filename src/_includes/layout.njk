<!DOCTYPE html>
<html lang="{{ language }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} - My Blog</title>
  <link rel="stylesheet" href="/assets/styles.css">
</head>
<body>
  <!-- Include the header -->
  {% include "header.njk" %}

  <main>
    <h1>{{ title }}</h1>

    <section class="blog-posts">
      {% for post in collections[language] %}
        <article class="post">
          <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          <p class="date">{{ post.date | date: "%B %d, %Y" }}</p>
          <p>{{ post.templateContent | safe | truncate(200) }} <a href="{{ post.url }}">Read more</a></p>
        </article>
      {% endfor %}
    </section>
  </main>

  <!-- Include the footer -->
  {% include "footer.njk" %}
</body>
</html>
